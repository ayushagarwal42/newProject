<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= isEditing ? 'Edit Listing' : 'New Listing' %></title>
</head>
<body>
    <h1><%= isEditing ? 'Edit Listing' : 'Create a New Listing' %></h1>
    <form action="<%= isEditing ? '/listings/' + listing._id + '?_method=PUT' : '/listings' %>" method="post">
        <div>
            <label for="title">Title:</label>
            <input type="text" id="title" name="title" value="<%= isEditing ? listing.title : '' %>" required>
        </div>
        <div>
            <label for="description">Description:</label>
            <textarea id="description" name="description" required><%= isEditing ? listing.description : '' %></textarea>
        </div>
        <div>
            <label for="image">Image URL:</label>
            <input type="text" id="image" name="image" value="<%= isEditing ? listing.image : '' %>">
        </div>
        <div>
            <label for="price">Price:</label>
            <input type="number" id="price" name="price" value="<%= isEditing ? listing.price : '' %>" required>
        </div>
        <div>
            <label for="location">Location:</label>
            <input type="text" id="location" name="location" value="<%= isEditing ? listing.location : '' %>" required>
        </div>
        <div>
            <label for="country">Country:</label>
            <input type="text" id="country" name="country" value="<%= isEditing ? listing.country : '' %>" required>
        </div>
        <button type="submit"><%= isEditing ? 'Update Listing' : 'Create Listing' %></button>
    </form>
    <a href="/listings">Back to Listings</a>
</body>
</html>
